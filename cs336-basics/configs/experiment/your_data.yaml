# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: /root/assignment4-data/cs336_data/data/train.bin
  
  # The validation set which we have already tokenized with GPT-2 tokenizer, don't change this!
  valid_bin: /root/assignment4-data/cs336_data/data/tokenized_paloma_c4_100_domains_validation.bin
  
  model_output: /root/assignment4-data/cs336_data/data/train

training:
  wandb_entity: 2310572998jin  # Set to your wandb entity, e.g. pliang
  wandb_project: cs336-data